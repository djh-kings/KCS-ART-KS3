# Archive

Superseded design mocks from the UX redesign. The live exhibit is `/index.html` on `main`; these are kept for reference only and are **not** the source of truth.

- `Hidden Patterns - Handoff.dc.html` — interactive design mock (visual spec).
- `Hidden Patterns - Standalone.html` — self-contained export of the mock.
